import 'package:flutter/material.dart';

class AppColors {
  // Brand Palette
  static const Color primaryColor = Color(0xFFc62828);       // Main brand color
  static const Color secondaryColor = Colors.black;  // Supporting brand color
  static const Color accentColor = Color(0xffffc107); // For highlights

  // Neutral Colors
  static const Color backgroundColor = Colors.white; // Default light background
  static const Color textColor = Colors.black;       // Default text in light mode
  static const Color grey = Colors.grey;             // For borders, hints, etc.

  // Feedback Colors
  static const Color errorColor = Colors.redAccent;  // Errors/alerts
  static const Color successColor = Colors.green;    // Success messages

  // Buttons
  static const Color buttonColor = Colors.white;       // Keep brand red on buttons
}
